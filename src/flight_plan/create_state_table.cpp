#include "utilities.h"

class FlightPlan {        
    public:
        FlightPlan(std::string pathToFlightPlan);
        ~FlightPlan();
};

FlightPlan::FlightPlan(std::string pathToFlightPlan) {
    // get the yaml file of the flight plan and states and everything 
}

FlightPlan::~FlightPlan() {
}
