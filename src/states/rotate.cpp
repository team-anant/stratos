#include <bits/stdc++.h>
#include "state.h"

class rotate : public state {
    void init() {
        // do init for rotate state
        raise(SIGSTOP);
        action();
    }
    void action() {
        // what will the state constantly be doing
        while(1) {
            // rotate
        }
        return;
    }
};

int main() {
    
    return 0;
}